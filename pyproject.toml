[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "snapir"
version = "0.1.0"
description = "Deterministic scanner/parser for Composer artifacts"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
