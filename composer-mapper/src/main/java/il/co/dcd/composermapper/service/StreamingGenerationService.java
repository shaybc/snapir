package il.co.dcd.composermapper.service;
import il.co.dcd.composermapper.index.Indexes; import il.co.dcd.composermapper.model.*; import il.co.dcd.composermapper.parser.*; import il.co.dcd.composermapper.render.*; import il.co.dcd.composermapper.util.FileUtil; import java.nio.file.Path; import java.io.IOException; import java.util.List;
public class StreamingGenerationService {
  private final OperationFileParser opParser; private final FormatFileParser fmtParser; private final ContextFileParser ctxParser; private final JavaClassParser javaParser; private final OperationMarkdownWriter opWriter; private final OpStepMarkdownWriter stepWriter; private final FormatMarkdownWriter fmtWriter; private final ContextMarkdownWriter ctxWriter; private final JavaClassMarkdownWriter javaWriter; private final LinkResolver links;
  public StreamingGenerationService(OperationFileParser opParser, FormatFileParser fmtParser, ContextFileParser ctxParser, JavaClassParser javaParser, OperationMarkdownWriter opWriter, OpStepMarkdownWriter stepWriter, FormatMarkdownWriter fmtWriter, ContextMarkdownWriter ctxWriter, JavaClassMarkdownWriter javaWriter){ this(opParser,fmtParser,ctxParser,javaParser,opWriter,stepWriter,fmtWriter,ctxWriter,javaWriter,new LinkResolver()); }
  public StreamingGenerationService(OperationFileParser opParser, FormatFileParser fmtParser, ContextFileParser ctxParser, JavaClassParser javaParser, OperationMarkdownWriter opWriter, OpStepMarkdownWriter stepWriter, FormatMarkdownWriter fmtWriter, ContextMarkdownWriter ctxWriter, JavaClassMarkdownWriter javaWriter, LinkResolver links){ this.opParser=opParser; this.fmtParser=fmtParser; this.ctxParser=ctxParser; this.javaParser=javaParser; this.opWriter=opWriter; this.stepWriter=stepWriter; this.fmtWriter=fmtWriter; this.ctxWriter=ctxWriter; this.javaWriter=javaWriter; this.links=links; }
  public void generate(List<DiscoveredFile> files, Indexes x, Path vault){ for(var f:files){ switch(f.type()){ case XML -> generateXml(f.path(),x,vault); case JAVA -> generateJava(f.path(),x,vault); case INI -> {} } } writeUnresolved(x,vault); }
  private void generateXml(Path xml, Indexes x, Path vault){ opParser.parse(xml).forEach(op->{ opWriter.write(op,x,links,vault); op.getSteps().forEach(s->stepWriter.write(s,x,links,vault)); }); fmtParser.parse(xml,x).forEach(f->fmtWriter.write(f,x,links,vault)); ctxParser.parse(xml).forEach(c->ctxWriter.write(c,vault)); }
  private void generateJava(Path javaFile, Indexes x, Path vault){ JavaClassDef d=javaParser.parse(javaFile); if(d!=null) javaWriter.write(d,x,links,vault); }
  private void writeUnresolved(Indexes x, Path vault){ StringBuilder sb=new StringBuilder(); sb.append("# Unresolved References\n\n## Unresolved Classes\n"); if(x.unresolvedClassRefs().isEmpty()) sb.append("- None\n"); else x.unresolvedClassRefs().forEach(v->sb.append("- `").append(v).append("`\n")); sb.append("\n## Unresolved Formats\n"); if(x.unresolvedFormatRefs().isEmpty()) sb.append("- None\n"); else x.unresolvedFormatRefs().forEach(v->sb.append("- `").append(v).append("`\n")); sb.append("\n## Unresolved Contexts\n"); if(x.unresolvedContextRefs().isEmpty()) sb.append("- None\n"); else x.unresolvedContextRefs().forEach(v->sb.append("- `").append(v).append("`\n")); try{ FileUtil.writeString(vault.resolve("analysis").resolve("unresolved-references.md"),sb.toString()); }catch(IOException e){ throw new RuntimeException(e);} }
}
